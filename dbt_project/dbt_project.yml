name: 'sales_pipeline'
version: '1.0.0'

profile: 'sales_pipeline'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
snapshot-paths: ["snapshots"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  sales_pipeline:
  
    staging:
      +materialized: view

    marts:
      +materialized: table